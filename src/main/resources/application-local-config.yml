server:
  address: 0.0.0.0

spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://rnd.daontec.co.kr:13306/wook_test1?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=Asia/Seoul
    username: cwryu
    password: cwryu1234

  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.MySQLDialect
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        ddl-auto: create

cloud:
  aws:
    s3:
      bucket: bucl-bucket

logging:
  level:
    com:
      amazonaws:
        util:
          EC2MetadataUtils: error
